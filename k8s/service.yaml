apiVersion: v1
kind: Service
metadata:
  name: netai-forecast-api
  namespace: netai
  labels:
    app: netai-forecast
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: netai-forecast
    component: api
